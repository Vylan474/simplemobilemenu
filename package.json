{
  "name": "menu-editor",
  "version": "1.0.0",
  "description": "A simple menu editor for restaurants and bars",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo 'Error: No tests specified. Add test framework.' && exit 1",
    "test:watch": "echo 'Error: No tests specified. Add test framework.' && exit 1",
    "lint": "echo 'Error: No linting configured. Consider adding ESLint.' && exit 1",
    "lint:fix": "echo 'Error: No linting configured. Consider adding ESLint.' && exit 1",
    "typecheck": "echo 'Error: No TypeScript configured. Consider adding TypeScript or JSDoc checking.' && exit 1",
    "format": "echo 'Error: No formatting configured. Consider adding Prettier.' && exit 1"
  },
  "keywords": [
    "menu",
    "restaurant",
    "editor"
  ],
  "dependencies": {
    "@vercel/blob": "^0.27.0",
    "@vercel/postgres": "^0.10.0",
    "bcryptjs": "^3.0.2",
    "express": "^4.18.2",
    "google-auth-library": "^10.3.0",
    "multer": "^2.0.2",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/uuid": "^10.0.0",
    "axe-core": "^4.10.3",
    "cli-table3": "^0.6.5",
    "lighthouse": "^12.8.2",
    "nodemon": "^3.0.1",
    "pa11y": "^9.0.0",
    "puppeteer": "^24.19.0"
  }
}
